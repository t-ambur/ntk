# Network Tool Kit

This is a command line tool (ntk) written in Rust to be a cross-platform solution to common networking discovery and diagnostics.

# Testing

```sh
cargo build
cargo run -- help
cargo run -- discover --help
cargo run -- discover
```
